<% if @stock.errors.any? %>
    <h2>The following erros prevented creating the new stock:</h2>
    <ul>
    <% @stock.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
<% end %>

<%= form_with(model: @stock, local: true) do |f| %>
    
    <p>
        <%= f.label :name %> <br>
        <%= f.text_field :name %>
    </p><br>
    <p>
        <%= f.label :description %>
        <%= f.text_area :description %>
    </p>

    <p>
        <%= f.submit %>
    </p>
<% end %>
